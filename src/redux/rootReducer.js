import {combineReducers} from "redux";
import ChangeDressReducer from "./ChangeDressReducer";

const rootReducer = combineReducers({
  //store  tổng của ứng dụng
  ChangeDressReducer,
});

export default rootReducer;
